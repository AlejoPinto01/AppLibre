import 'package:flutter/material.dart';

class CuponsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(child: AppBar(title: Text('Cupones'),),);
  }
}