import 'package:flutter/material.dart';

class Category {
  String name;
  Icon icon;
  String imageURL;
  Color color;

  Category(
      {required this.name,
      required this.icon,
      required this.imageURL,
      required this.color});
}
